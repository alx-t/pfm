spring:
  application:
    name: pfm-service
  datasource:
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5434/pfm
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
#  flyway:
server:
  port: 8081
